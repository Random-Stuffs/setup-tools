apiVersion: v1
kind: Service
metadata:
  name: mempalace
  namespace: mcp
spec:
  selector:
    app: mempalace
  ports:
  - port: 3000
    targetPort: 3000
  clusterIP: None   # Headless — required for StatefulSet stable DNS (mempalace-0.mempalace.mcp)
