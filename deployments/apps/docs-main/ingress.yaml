apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: docs-main
  namespace: apps
spec:
  entryPoints:
  - web
  routes:
  - match: Host(`docs.homelab.local`)
    kind: Rule
    services:
    - name: docs-main
      port: 80
